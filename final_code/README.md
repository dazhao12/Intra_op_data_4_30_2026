# final_code / 最终代码视图

## 中文

这里按 01-06 展示主流程代码。final 视图只保留当前主线代码；`submit_*.sh` 集群包装脚本、QC/diagnostic 脚本、旧迭代版本均归档在 `../archive/`。

当前最终执行入口：

- `RUN_FINAL_STEP_build_intraop_timeseries_model_ready_v8.py`
- 等价路径：`06_final_model_ready/build_intraop_timeseries_model_ready_v8.py`

## English

This folder shows the main workflow code in 01-06 order. The final view keeps only the current mainline code; `submit_*.sh` cluster wrappers, QC/diagnostic scripts, and older iterations are archived under `../archive/`.

Current final executable entry point:

- `RUN_FINAL_STEP_build_intraop_timeseries_model_ready_v8.py`
- Equivalent path: `06_final_model_ready/build_intraop_timeseries_model_ready_v8.py`
